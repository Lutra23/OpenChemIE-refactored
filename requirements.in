# OpenChemIE Refactored - Core Dependencies

# --- 核心 AI & 机器学习 ---
torch~=1.8.0
torchvision~=0.9.0
huggingface_hub

# --- 计算机视觉 & 图像处理 ---
opencv-python~=4.5.0
Pillow~=8.0.0
layoutparser

# --- PDF 处理 ---
pdf2image
PyPDF2
pdfminer.six
pdftotext
pymupdf~=1.18.0 # 保留自旧文件，可能被间接使用

# --- 化学信息学 ---
rdkit-pypi
MolScribe @ git+https://github.com/CrystalEye42/MolScribe.git@250f683
RxnScribe @ git+https://github.com/Ozymandias314/MolDetect.git
ChemIENER @ git+https://github.com/Ozymandias314/ChemIENER.git
chemrxnextractor @ git+https://github.com/CrystalEye42/ChemRxnExtractor.git@0f9529d

# --- 数据处理 & 工具 ---
numpy~=1.20.0
pandas~=1.3.0
psutil
matplotlib

# --- FastAPI 后端服务 ---
fastapi~=0.100.0
uvicorn[standard]~=0.20.0
python-multipart  # 用于文件上传

# --- 异步任务 & 缓存 ---
celery
redis
fastapi-cache2[redis]

# --- 原 OpenChemIE 依赖 ---
# OpenChemIE 主包现在是 app.core 的一部分，不再需要通过git安装。
# 旧依赖中的 jupyter 和 matplotlib 作为可选的开发工具，暂时不加入核心依赖。

# 可选：Jupyter支持
jupyter~=1.0.0
matplotlib~=3.3.0

# 可选：GPU支持（根据需要选择其中一个）
# torch[gpu]  # 对于较新的PyTorch版本
# torch-gpu   # 对于较老的版本

# 注意：OpenChemIE主包需要单独安装：
# pip install 'OpenChemIE @ git+https://github.com/CrystalEye42/OpenChemIE' 